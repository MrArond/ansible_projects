- name: Upgrade Fedora System
  hosts: localhost
  become: true
  tasks:
    - name: Upgrade all packages
      ansible.builtin.dnf:
        name: '*'
        state: latest
        use_backend: dnf5
